import { round, matrix, multiply, cos, sin, pi, tau, nthRoot } from "mathjs";
import Sphere from "../lib/sphere";

// TODO: delete this file after the explore page is gone
const Sphere2 = Sphere;
export default Sphere2;
